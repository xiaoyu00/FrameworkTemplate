<resources>


    <!-- 自定义loading动画 -->
    <style name="custom_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- Dialog的windowFrame框为无 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否漂现在activity上 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 是否半透明 -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 去除黑色边框的关键设置项 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 屏幕背景是否变暗 -->
        <item name="android:backgroundDimAmount">0.6</item>
    </style>

    <!-- 分割线 -->
    <style name="spaceLine">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0.5dp</item>
        <item name="android:background">#e0e0e0</item>
    </style>
    <!--  底部菜单栏  默认PrimarySurface 彩色Colored-->
    <!-- itemTextColor itemTextAppearanceActive itemTextAppearance -->
    <style name="NavigationStyle" parent="Widget.MaterialComponents.BottomNavigationView.Colored">
        <item name="labelVisibilityMode">labeled</item>
        <item name="itemIconTint">@null</item>
        <item name="badgeStyle">@style/BadgeStyle</item>
    </style>
    <!--  底部导航栏  角标-->
    <style name="BadgeStyle">
        <item name="badgeTextColor">@color/white</item>
        <item name="backgroundColor">@color/blue</item>
        <item name="maxCharacterCount">3</item>
        <item name="badgeGravity">TOP_END</item>
        <item name="verticalOffset">4dp</item>
    </style>
</resources>
